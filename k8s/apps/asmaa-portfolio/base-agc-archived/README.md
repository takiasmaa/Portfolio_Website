# Archived: AGC HTTPRoute

This HTTPRoute was used by the previous Azure Application Gateway for Containers architecture.

Previous path:
Cloudflare -> Azure Application Gateway for Containers -> Gateway API -> HTTPRoute -> Service -> Pod

It is archived because the live platform is moving away from AGC to reduce idle cost.
