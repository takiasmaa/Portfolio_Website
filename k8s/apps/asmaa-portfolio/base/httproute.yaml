apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: asmaa-portfolio-route
spec:
  parentRefs:
    - name: asmaa-portfolio-gateway
  rules:
    - backendRefs:
        - name: asmaa-portfolio
          port: 80